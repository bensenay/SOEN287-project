# Server
PORT=5050

# MySQL (XAMPP defaults — change DB_PASSWORD if you set one in phpMyAdmin)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=smart_course_companion

# JWT — replace this with a long random string before sharing or deploying
JWT_SECRET=change_this_to_a_long_random_secret